require: standard
inherit_gem:
  standard: config/base.yml
AllCops:
AllCops:
  TargetRubyVersion: 4.0
  NewCops: enable
  Exclude:
    - "vendor/bundle/**/*"
    - "tmp/**/*"
    - "public/**/*"
    - "bin/{bundle,rails,rake,spring}"
